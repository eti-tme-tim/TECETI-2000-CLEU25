# Automation for create dcloud kubernetes clusters

## Authentication

Extract your current web browser key via:

![alt text](images/dcloud-topo-auth.png)

And export it via CLI using this environment variable.

```bash
export TB_AUTH_TOKEN=...
```

Note: the key expires fairly frequently.
